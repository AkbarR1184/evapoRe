% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_exeves.R
\name{plot_exeves}
\alias{plot_exeves}
\title{Plot Extreme Evaporation Events}
\usage{
plot_exeves(
  x,
  my_palette = c("#4D648D", "#337BAE"),
  event_color = "#a9cce0",
  title = NULL,
  shapes = c(0, 16, 15)
)
}
\arguments{
\item{x}{A data.table (see details)}

\item{my_palette}{Character vector of 2 colors: (1) extreme, (2) event}

\item{event_color}{Character; color for x-axis event markers (default = "#a9cce0")}

\item{title}{Optional character string; plot title}

\item{shapes}{Numeric vector of 3 shape codes (see details)}
}
\value{
A ggplot object
}
\description{
Convenient and aesthetic visualization of extreme evaporation events.
}
\details{
`x` must be a `data.table` with the following columns:
- `"date"`: Date of observation  
- `"value"`: Evaporation value  
- `"extreme_id"`: Identifier for extreme evaporation events (can be `NA`)  
- `"above_low_thres_id"`: Identifier for values above a lower threshold  
- `"event_id"`: Identifier for broader event periods (can be `NA`)  

`my_palette` is a character vector of two colors specifying the color for  
extreme events and broader events, respectively.

`event_color` defines the color for marking event periods along the x-axis.

`shapes` is a numeric vector of three shape codes for:  
(1) extreme points, (2) event points, and (3) event markers on the x-axis.
}
\examples{
\donttest{

# Create example data
dt <- data.table(
  date = seq(as.Date("2020-01-01"), as.Date("2020-03-01"), by = "day"),
  value = runif(61, 0, 5),
  extreme_id = c(rep(NA, 30), 1, NA, NA, 2, rep(NA, 26), 3),
  above_low_thres_id = NA,
  event_id = c(rep(NA, 25), rep(1, 10), rep(NA, 26))
)

plot_exeves(dt)
}
}

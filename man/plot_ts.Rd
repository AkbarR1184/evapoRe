% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ts.R
\name{plot_ts}
\alias{plot_ts}
\title{Plot Time Series}
\usage{
plot_ts(
  x,
  type_analysis = c("single", "multi"),
  show_median = FALSE,
  colors = NULL,
  title = "Time Series Plot",
  x_label = "Time (year)",
  y_label = "Value",
  x_breaks = NULL,
  x_text = TRUE,
  y_text = TRUE,
  legend = TRUE
)
}
\arguments{
\item{x}{A `data.table` or `data.frame` with columns: "date", "value", "name".}

\item{type_analysis}{Character, either "single" or "multi". Determines if one or multiple
series are being plotted.}

\item{show_median}{Logical. If TRUE (and type_analysis is "multi"), a dashed black
median line across series is added. Default is FALSE.}

\item{colors}{Optional vector of colors. Can be named or unnamed.}

\item{title}{Character. Plot title. Default is "Time Series Plot".}

\item{x_label}{Character. X-axis label. Default is "Time (year)".}

\item{y_label}{Character. Y-axis label. Default is "Value".}

\item{x_breaks}{Character. ggplot2-compatible time break (e.g., "1 year", "3 months").}

\item{x_text}{Logical. Show x-axis text? Default is TRUE.}

\item{y_text}{Logical. Show y-axis text? Default is TRUE.}

\item{legend}{Logical. Show legend? Default is TRUE.}
}
\value{
A ggplot object
}
\description{
Generate a line plot from tidy time-series data with optional customization for
multi-series datasets, automatic frequency detection, and optional median line.
}
\details{
The function supports plotting of daily, monthly, or yearly time series. It detects
time frequency based on the first two dates of one dataset. If the year difference is 1,
the data is treated as yearly; otherwise, it uses monthly formatting ("%Y-%m").
Optionally, a black dashed median line can be plotted when multiple series exist.
}

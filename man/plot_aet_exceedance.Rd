% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_aet_exceedance.R
\name{plot_aet_exceedance}
\alias{plot_aet_exceedance}
\title{Plot AET > PET Exceedance Frequency}
\usage{
plot_aet_exceedance(
  x,
  facet_by = c("none", "month", "season"),
  title = NULL,
  palette = NULL,
  legend_title = "PET Method",
  legend_position = "right",
  x_label = NULL,
  y_label = "Exceedance Frequency",
  use_google_font = TRUE,
  font_family = "Lato",
  font_size = 13
)
}
\arguments{
\item{x}{A `data.table` with columns: `lon`, `lat`, `date`, `aet`, `pet`.
Optionally: `dataset` and `method`.}

\item{facet_by}{Character. One of `"none"`, `"month"`, or `"season"`. Default = `"none"`.}

\item{title}{Optional character. Title of the plot. If `NULL`, a default is generated.}

\item{palette}{Optional named character vector of colors for PET methods.
If `NULL`, the `scico` palette `"roma"` is used.}

\item{legend_title}{Character. Legend title. Default = `"PET Method"`.}

\item{legend_position}{Character. Legend position. Default = `"right"`.}

\item{x_label}{Character. X-axis label. Default = `NULL`.}

\item{y_label}{Character. Y-axis label. Default = `"Exceedance Frequency"`.}

\item{use_google_font}{Logical. Use Google Fonts. Default = `TRUE`.}

\item{font_family}{Character. Font family name. Default = `"Lato"`.}

\item{font_size}{Numeric. Base font size. Default = `13`.}
}
\value{
A `ggplot` object.
}
\description{
Generates a grouped bar plot showing the average spatial frequency
at which actual evapotranspiration (AET) exceeds potential evapotranspiration (PET),
optionally grouped by dataset, PET method, and time (month or season).
}
\details{
If the input includes `dataset` (AET data source) and `method` (PET method),
bars are grouped by dataset and filled by PET method. The function supports
monthly or seasonal faceting. Output is a ggplot2 object.
}

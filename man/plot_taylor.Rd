% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_taylor.R
\name{plot_taylor}
\alias{plot_taylor}
\title{Plot Taylor Diagram for Model Evaluation}
\usage{
plot_taylor(
  x,
  type = "default",
  group = "dataset",
  palette = "roma",
  shapes = NULL,
  normalise = FALSE,
  lon_range = NULL,
  lat_range = NULL,
  ...
)
}
\arguments{
\item{x}{A `data.table` with columns: `lon`, `lat`, `date`, `value`, `dataset`, and optionally `source`.}

\item{type}{Character or character vector for faceting, e.g. "season", "site". Default is "default".}

\item{group}{Character or character vector for grouping. Default is "dataset".}

\item{palette}{Character. A scico palette name or color vector. Default is "roma".}

\item{shapes}{Integer vector of point shapes. Default is NULL (auto).}

\item{normalise}{Logical. If TRUE, normalize SD and RMSD. Default is FALSE.}

\item{lon_range}{Numeric vector of length 2 for filtering by longitude. Default is NULL.}

\item{lat_range}{Numeric vector of length 2 for filtering by latitude. Default is NULL.}

\item{...}{Additional arguments passed to `TaylorDiagram()`.}
}
\value{
An openair object (S3), including the Taylor plot and processed data.
}
\description{
Generate a Taylor diagram from tidy climate model and observation data with full
support for grouping, seasonal faceting, normalization, and spatial site selection.
}
\details{
This function wraps openair's TaylorDiagram to provide a streamlined interface for
plotting model evaluation metrics. It accepts a tidy data.table and supports:

- Grouping by model name or other attributes via `group`
- Faceting by time, season, or spatial location via `type`
- Custom color palettes and point shapes
- Optional filtering by longitude and latitude range

Input `x` must be a long-format `data.table` with at least: `lon`, `lat`, `date`, `value`, and `dataset`.
It should include both observed and model values in the same column (`value`), distinguished by `dataset`.
}
